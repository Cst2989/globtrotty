<!-- the candidate arm of the release canary (src/loop/release.ts).
  Written by hand rather than by npm run examples, because the hypothesis under
  test is about the sentence and not about the selection: the control file's
  examples say what a finished trip looks like, and this one asks the desk to
  reach for the closest of them. The selection block below stays byte-identical
  to the control file, so the only difference between the two arms is the
  sentence, which is what makes a difference in conversion attributable to it.
-->
Examples of trips this traveller accepted as proposed. They are examples of shape
and not of price: search for her own dates and quote what the search returns.
When her request matches the shape of one of these closely, propose that shape
first rather than building a new one.
